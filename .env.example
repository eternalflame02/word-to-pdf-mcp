AUTH_TOKEN=your_secret_token_here
MY_NUMBER=919876543210
FILES_DIR=files
# Set to your public base URL (e.g., ngrok URL) so file_url is returned
# Example: https://example.ngrok-free.app
BASE_URL=
# If true, also include pdf_base64 in responses (can be large)
INCLUDE_BASE64=false

# If Puch passes attachment IDs, set a template to download the file by ID.
# Example: https://api.puch.ai/files/{id}
PUCH_DOWNLOAD_URL_TEMPLATE=
# Optional auth token header when fetching by ID
PUCH_API_TOKEN=